pub function factorial(Int n) -> Int
  returns match n
    when 0 -> 1
    otherwise -> n * factorial(n - 1)

pub function fibonacci(Int n) -> Int
  returns match n
    when 0 -> 0
    when 1 -> 1
    otherwise -> fibonacci(n - 1) + fibonacci(n - 2)
